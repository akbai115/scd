
export interface FabricUniforms {
  uTime: { value: number };
  uMouse: { value: [number, number] };
  uGhostPos: { value: [number, number, number] };
  uResolution: { value: [number, number] };
  uClickImpulse: { value: number };
  uAudio: { value: number };
}
